[tool.poetry]
name = "snowflake.py"
version = "1.0.0"
description = ""
authors = ["Ricky <ricky@skyrat.dev>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
